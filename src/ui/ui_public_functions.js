// editor is a global variable defined in another file

function get_code() {
    return getEditorCode();
}

function update_variables(variables) {
    console.log(variables);
    draw_visualisation(variables);
}

function update_statistics(statistics) {
    console.log(statistics);
}

function show_error(message) {
    alert("Error from code: " + message);
}

function show_message(message) {
    alert("Print from code: " + message);
}

function show_message(message) {

}

function set_current_line(line_number) {
    return
}

