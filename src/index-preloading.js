// function checkPyScriptReady() {
//     // Add an event listener for console messages
//     window.addEventListener('message', function (event) {
//         // Check if the specified message is logged
//         if (event.data && event.data.indexOf('[pyscript/main] PyScript Ready') !== -1) {
//             // Display a popup indicating that the app is ready
//             alert('PyScript is Ready!');
//         }
//     });
// }

// // Call the function to start monitoring the console
// checkPyScriptReady();